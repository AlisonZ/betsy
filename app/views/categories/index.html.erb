<h1>All Categories</h1>
<% @categories.each do |category| %>
    <!-- need to link to list of products with this category -->
    Category: <%= link_to category.name, category_products_path(category.id) %>
<% end %>

<h1>All Users</h1>

<% if !@users.any? %>
<p>
  No users found.
</p>
<% else %>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= link_to user.username, user_products_path(user.id) %>
    <%# user.email %>
  </li>

  <% end %>
</ul>
<% end %>

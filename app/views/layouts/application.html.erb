<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://fonts.googleapis.com/css?family=Codystar" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Cormorant+Garamond" rel="stylesheet">


  <title>Debtsy</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
</head>

<body>
  <header>
    <div class="row" id="top-row">
      <h1 class="main-header small-4 columns"><%= link_to "Debtsy", root_path %></h1>
      <div class="cart small-7 columns">
        <%= link_to image_tag( "cart"), cart_path %>
        <%= total_items_in_cart %>
      </div>
    </div>
    <div class="row">
      <div class="small-12 large-6 columns shop-by">
        <ul>
            <li>
                <%= link_to "shop by category", categories_path %>
            </li>
            <li>
                <%= link_to "shop by merchant", users_path %>
            </li>
            <li>
                <%= link_to "see all products", products_path %>
            </li>
        </ul>
      </div>
      <nav class="small-12 large-6 columns" id="nav-bar">
        <% if current_user %>
        <ul class="header-login">
          <li>
            Logged in as <%= current_user.username %>
          </li>
          <li>
            <%= button_to "My account", user_path(current_user.id), method: :get, class: "button" %>
          </li>
          <li>
            <%= button_to "Log out", logout_path, method: :delete, class: "button" %>
          </li>
        </ul>
        <% else %>
        <ul class="header-login">
          <li>
            <%= button_to "Log in", '/auth/github', class: "button" %>
          </li>
        </ul>
        <% end %>
      </nav>
    </div>
  </header>
  <main class="content">
    <section class="flash">
      <% flash.each do |name, message| %>
      <h3 class="row status <%= name %>"><%= message %></h3>
      <% end %>
    </section>
    <%= yield %>
  </main>
  <footer>
    <div class="row">
      <p class="small-12 column">
        Copyright 2017 Jolly Owl
      </p>
    </div>
  </footer>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
  <title>d'Ebtsy</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <header>
    <h1 class="main-header"><%= link_to "d'Ebtsy", root_path %></h1>
    <ul class="header-nav">
      <li>
        <% if current_user %>
        <p>
          Logged in as <%= current_user.username %>
        </p>
        <%= button_to "Log out", login_path, method: :delete %>
        <% else %>

        <%= button_to "Log in", login_path, method: :get %>
        <% end %>
      </li>
      <li>
        <%= image_tag "cart", class: "cart" %>
      </li>
    </ul>

  </header>
  <body class="content">
    <section class="flash">
      <% flash.each do |name, message| %>
      <h3 class="row status <%= name %>"><%= message %></h3>
      <% end %>
    </section>
    <%= yield %>
  </body>
</body>
</html>
